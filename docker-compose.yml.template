version: '3'

services:
  stobot:
    build: .
    environment:
      - DISCORD_TOKEN=<your discord token>
    volumes:
      - <path to channels txt folder>:/stobot
    command:
      - -c /stobot/channels.txt